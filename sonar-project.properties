sonar.projectKey=danstis_ai-usage-dashboard
sonar.organization=danstis

# This is the name and version displayed in the SonarCloud UI.
#sonar.projectName=ai-usage-dashboard
#sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
#sonar.sources=.

# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8

# Go coverage report produced by the test step
sonar.go.coverage.reportPaths=coverage.out

# Exclude Go test files from coverage analysis: SonarCloud would otherwise
# count the test functions themselves as uncovered new code (they're not
# production code), dragging down "Coverage on New Code" by lines that
# cannot meaningfully be covered. The tests still run and produce the
# coverage report that measures the production code.
sonar.coverage.exclusions=**/*_test.go

# Explicit Go test identification — without these, SonarCloud's automatic
# language-based test detection sometimes folds *_test.go into the
# production source set, which pollutes the production-code quality gate
# with cognitive-complexity / coverage / naming findings that are only
# relevant to test code. Dan's review (BSOD-68 follow-up) asked for the
# standard Go pattern, kept co-located with the coverage exclusion so the
# two related settings live next to each other.
sonar.tests=.
sonar.test.inclusions=**/*_test.go
sonar.test.exclusions=**/*_test.go
