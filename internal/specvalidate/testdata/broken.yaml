openapi: 3.0.3
info:
  title: Broken Spec Fixture
  version: "1.0.0"
paths:
  /broken:
    get:
      responses:
        "200":
          description: broken
          content:
            application/json:
              schema:
                type: not-a-real-type
